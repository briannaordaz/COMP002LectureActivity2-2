// Lecture 2-2
// Show assigning strings, Booleans, numbers to variable.
// Show a  constant variable assignment. 
// Show what happens when you try to override it.
//Show calling console.log() and Math.min(), and Math.max().

// string variable
let myVariable = "Hello World!"
console.log(myVariable);

// change the string variable value
myVariable = "I changed the value of the string variable here.";
console.log(myVariable);

// const variable
const pi = 3.14;
// pi = 2; // throws an error

console.log(Math. min(pi, 2 ));

